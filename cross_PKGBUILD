# Maintainer: VHSgunzo <vhsgunzo.github.io>
pkgname='Run-wrapper'
pkgver='0.0.5'
pkgrel='1'
pkgbase="${pkgname}"
pkgdesc='Launching an executable file with environment variables from a specific process id'
arch=("aarch64" "x86_64")
url="https://github.com/VHSgunzo/${pkgname}"
provides=("${pkgname}")
conflicts=("${pkgname}")
license=('MIT')
source=("https://github.com/Azathothas/${pkgname}/releases/download/v${pkgver}/${pkgname}-${CARCH}-Linux")
sha256sums=('SKIP')

package() {
    install -Dm755 "${pkgname}-${CARCH}-Linux" "${pkgdir}/var/RunDir/Run"
}